import React from 'react';
import { Flex, Heading, Image, Button, flexbox } from "@chakra-ui/react";
import { FaInstagram } from 'react-icons/fa';
import { Layout } from '../components';

export const Home: React.FC<any> = (props: any) => {


    const TestRows = () => {
        return (
            <div style={{height: '18vh', borderBottom: '1px grey solid'}}>
                hello
            </div>
        );
    }
    

    return (
        <Layout>
        </Layout>
    );
}

